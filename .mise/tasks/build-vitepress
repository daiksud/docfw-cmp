#!/usr/bin/env bash
#MISE description="Build the VitePress static site"

set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo_root/frameworks/vitepress"

bun install --frozen-lockfile
bun run build

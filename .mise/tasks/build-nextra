#!/usr/bin/env bash
#MISE description="Build the Nextra static site"

set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo_root/frameworks/nextra"

bun install --frozen-lockfile
bun run build
